# Core dependencies (versions from README)
torch==2.2.2
numpy==1.24.2
scipy==1.10.1
gym==0.25.2
tensorboard==2.9.0
protobuf==3.20.3  # tensorboard compatibility on newer Python/protobuf releases

# MuJoCo support for standard benchmarks (HalfCheetah, etc.)
# If you already have MuJoCo installed system-wide, you can comment these out.

mujoco==3.1.1
gymnasium[mujoco]


# Optional: DeepMind Control / MetaWorld variants
#dm-control
#metaworld
